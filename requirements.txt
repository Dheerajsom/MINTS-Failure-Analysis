# Prefer `pip install -e .` (see pyproject.toml). This file mirrors the core
# analysis dependencies for environments that expect a requirements.txt.
numpy
scipy
pandas>=2.0
matplotlib

# Optional extras (installed via `pip install -e ".[sensor]"` / ".[dev]"):
#   sensor: pyserial, paho-mqtt, getmac, pynmea2, netifaces, pyyaml
#   dev:    pytest
